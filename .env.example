# Database Configuration (PostgreSQL required for production)
# Connection pooling parameters (optional):
# - pool_max_size: Maximum number of connections in the pool (default: 10)
# - pool_min_size: Minimum number of connections in the pool (default: 0)
# - pool_acquire_timeout: Timeout in seconds for acquiring a connection (default: 30)
# - pool_idle_timeout: Timeout in seconds for idle connections (default: 600)
DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require&pool_max_size=20&pool_min_size=2

# Ponder schema name (used for production)
PONDER_SCHEMA=ponder_prod

# Port configuration (default: 42069 for Fly.io)
PORT=42069

# RPC URLs for Worldchain Sepolia chain
PONDER_RPC_URL_WORLD_TESTNET_1=https://your-rpc-endpoint-1
